// optimizer config:
// Ordenar bien esto para no cometer desastres.
{
    "appDir": "../www",
    "mainConfigFile": "../www/app.js",
    "dir": "../www-built",
    "modules": [
        {
            "name": "app"
        }
    ]
}

// To optimize, run:
// node tools/r.js -o tools/build.js